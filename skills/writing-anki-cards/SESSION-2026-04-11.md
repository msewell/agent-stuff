# Session Summary — 2026-04-11

## Goal
Improve `skills/writing-anki-cards/SKILL.md` using competitive skill research, then run the workflow against a real source with Anki/AnkiConnect.

## Key outcomes

### 1) Competitive research completed
- Used `npx skills find` to identify Anki/flashcard-related skills.
- Downloaded/analyzed skills from multiple repos and `skills.sh` pages.
- Compared strengths (safety, workflow rigor, formats, validation, reporting).

### 2) `SKILL.md` heavily refactored
File: `skills/writing-anki-cards/SKILL.md`

Major changes:
- Scope narrowed to **add-only** workflow.
- Added **script-first** canonical execution path.
- Added deterministic rules for:
  - preflight
  - deck handling
  - tags
  - hashing
  - chunking/resume
  - reporting
- Added compact final report format.
- Kept references for card-quality heuristics.

### 3) New bundled deterministic tool added
File: `skills/writing-anki-cards/scripts/anki_add_pipeline.py`

Subcommands:
- `preflight`
- `add-notes`
- `resume-status`

Handles:
- AnkiConnect + model/field preflight
- Deck auto-create
- Deterministic source hash and run tags
- Tag sanitization
- YAML checklist state + resume
- Duplicate preflight (`canAddNotesWithErrorDetail`)
- Batched insertion
- Newline normalization (`\n` → `<br>`)
- Soft warning counts
- Corrupt checklist quarantine (`*.corrupt.<timestamp>.yaml`)

### 4) Cloze field discrepancy investigated and resolved
- Observed runtime default in this environment: Cloze fields are `Text` + `Back Extra`.
- Verified with AnkiConnect and stock model metadata.
- Implemented strict policy per decision: Cloze uses `Back Extra` (not `Extra`).

### 5) Real environment setup and execution
- Installed Anki via Homebrew Cask.
- User installed/configured AnkiConnect.
- Ran real pipeline on gist content.
- Successful adds completed and verified via batch-tag queries.

## Notable run artifacts
- Source file used: `/tmp/connection-and-influence.md`
- Test notes file: `/tmp/anki-notes-pipeline-test.json`
- Example checklist: `/tmp/anki-add-run-b8b71b550f-connection-and-influence.pipeline-test.yaml`

## Final status
- Skill is now script-driven, deterministic, resume-aware, and validated in a live Anki environment.
- Remaining optional improvements discussed: dry-run mode, rollback command, checklist auto-versioning, transient retries, notes fingerprinting.
